# Copy to .env and adjust values as needed
ENVIRONMENT=local
SECRET_KEY=change-me

# Default matches docker-compose network
DATABASE_URL=postgresql+psycopg2://app:app@db:5432/app

# Postgres container config
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app

